# C54-DP1B — Project H56 as A56 Threshold Function

**Artifact role:** Markdown companion to one Shared Transformation Record operation occurrence  
**Operation:** `PROJECT_AS`  
**Selected Output Class:** `admissible_with_bounded_claim`  
**YAML Record:** [`C54-DP1B_PROJECT_AS_A56_Threshold_Function.yaml`](../yaml/C54-DP1B_PROJECT_AS_A56_Threshold_Function.yaml)  
**Package narrative:** [`C54-DP1_DECOMPOSE_to_PROJECT_AS_Threshold_Function_Chain.md`](../packages/C54-DP1_DECOMPOSE_to_PROJECT_AS_Threshold_Function_Chain.md)  

```text
record companion
≠ duplicate theory source
≠ independent operation occurrence
≠ replacement for the YAML record
```

## 1. Claim

Within target context A56, the origin-typed finer H56 reconstruction performs a bounded correction-window threshold function because the remaining window, closure point, and residual-load relation jointly change when escalation review becomes reconstructively relevant; no projection is retained in comparison context B56.

## 2. Source and Target

**Source object:** `reconstruction.H56.finer` — Origin-typed finer reconstruction from C54-DP1A.

**Source type:** `finer relational reconstruction of the same reference object`

**Target object:** `target-function.A56.threshold` — Bounded correction-window threshold function in A56.

**Contextual target function:** `correction-window threshold function in A56`

## 3. Operation Occurrence

**Occurrence ID:** `project-as.c54-dp1b.01`

**Transformation context:** C54-DP1 integrated source-to-function chain.

**Justification:** The independently warranted H56f source is tested for one bounded target function in A56.

**Expected praxeological difference:** The projection changes reconstruction of when correction remains open versus when residual review becomes relevant in A56.

## 4. Local Adjudication

**Local result:** H56f performs one bounded A56 correction-window threshold function.

**Canonical mapping:** `admissible_with_bounded_claim`

**Class-selection rationale:** The claim passes only as a context-bound source-sensitive function.

## 5. Loss Record

### `preserved`

- H56 origin type and finer phases: The source remains an occurrence reconstruction.
- Inherited C54-DP1A Loss: Window duration and source limits remain visible.

### `compressed`

- Target-irrelevant phase microdetail: Only threshold-relevant source features are foregrounded.

### `excluded`

- B56 transfer and prediction: No projection in source-insensitive B56 and no forecast claim.

### `uncertain`

- Exact threshold timing: Window duration remains uncertain.

### `irrecoverable`

- none declared

## 6. Stop and Non-Capture Boundary

**Stop reached:** `false`

**Stop rationale:** The A56 function is adequately captured; further transfer or action use requires new authority and source.

**Non-Capture asserted:** `false`

**Non-Capture rationale:** The A56 function is adequately captured; further transfer or action use requires new authority and source.

## 7. Authority Boundary

The projection authorizes no escalation, sanction, or application decision.

```yaml
governance:
  authority_inheritance: prohibited
```

## 8. Package Relation

The full shared source narrative, chain handoffs, comparison conditions, and adjacent operation occurrences remain in [`C54-DP1_DECOMPOSE_to_PROJECT_AS_Threshold_Function_Chain.md`](../packages/C54-DP1_DECOMPOSE_to_PROJECT_AS_Threshold_Function_Chain.md). This companion isolates only `C54-DP1B` so that every YAML operation Record has one directly corresponding Markdown artifact.
