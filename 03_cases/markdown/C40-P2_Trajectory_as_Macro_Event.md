# C40-P2 — Trajectory as Macro-Event

**Case class:** `positive_case`  
**Chapter owner:** Chapter 40 — RETYPE Cases, Countercases, and Local Audit  
**Operation:** `PROJECT_AS`  
**Local result:** non-punctualized wider-path Macro-Event function with origin type and internal duration preserved  
**Canonical Output Class:** `admissible_with_bounded_claim`  
**Contract status:** Chapter-40 Contract-minimum package 4 of 7  
**Artifact status:** instantiated, schema-validated, twelve-stage audited, mapped, and indexed

## 1. Governing Claim

Within synthetic wider institutional path `W2` from `Q-1` through `Q8`, the independently reconstructed `Q0–Q5` workload-review Trajectory from `C17-LAMBDA-01` performs one bounded Macro-Event function for the transition from ordinary review-correctable coordination to redesign-dependent coordination.

Its internal sequence remains constitutive:

```text
baseline
→ missed review
→ persistent workaround
→ asymmetric load shift
→ default stabilization
→ partial repair
```

The source remains a temporally extended Trajectory. It does not become one punctual Event, a complete historical period, a Macro-Event primitive, or a causal turning-point proof.

```text
Trajectory as origin type
+ bounded transition-bearing function in W2
≠ punctual Event identity
≠ causal necessity
≠ authority increase
```

## 2. Source and Claim Boundary

The source is [`C17-LAMBDA-01`](C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md), an independently typed `COMPOSE` result spanning `Q0–Q5`. It contains:

- a declared baseline arrangement;
- a warrantable central review Non-Event at `Q1`;
- positive preparation events inside that interval;
- a persistent workaround;
- an asymmetric workload shift;
- default stabilization through `Q4`;
- partial rather than complete repair at `Q5`;
- retained uncertainty and irrecoverable mechanism limits.

The current claim is limited to:

- wider target path `W2`;
- target period `Q-1–Q8`;
- one transition dimension: review-correctable to redesign-dependent coordination;
- one source Trajectory, with its locked `Q0–Q5` boundary preserved;
- one distinct `PROJECT_AS` occurrence.

It excludes other periodizations, other paths, complete-period identity, sole-cause status, necessity, person-level claims, legitimacy, recommendation, sanction, and application authority.

## 3. Wider Target Path and Comparison Path

`W2` is wider than the source Trajectory:

- at `Q-1`, ordinary review remains a low-cost correction route;
- `Q0–Q5` contains the source Trajectory;
- at `Q6–Q8`, residual default and incomplete repair make formal redesign a materially different continuation branch.

Comparison path `Z2` contains a timely review, effective redistribution, full reset, and a later independent rule change. In `Z2`, present conditions carry the redesign branch without additional source-dependent Macro-Event work.

This contrast prevents the projection from collapsing into the statement that a historically prominent period “was a turning point.” The tested difference is whether the source-sensitive extended course changes the wider reconstruction of later branch availability.

## 4. Tested Projection

### Load-bearing source traces

1. **Ordered phase sequence** — the source is not an event list; its phases and relations form one extended Trajectory.
2. **Central Non-Event** — the missed `Q1` review begins the shift away from ordinary review-correctability only through its later carryover.
3. **Workaround, asymmetry, and default stabilization** — the `Q2–Q4` relation closes the low-cost ordinary-review branch and sediments redesign pressure.
4. **Partial repair** — `Q5` reverses part of the load but leaves residual conditions; it prevents false homogeneity and false final restoration.

### Target-side praxis difference

The projection changes the wider-path reconstruction from:

```text
later redesign as an isolated present decision
```

into:

```text
later redesign as a distinct continuation branch
conditioned by one extended, internally heterogeneous source course
```

The function is transition-bearing. It is not a claim that the source was the sole cause or that one date was “the real event.”

## 5. Punctualization and Counterfactual Test

The claim is `strongly_sensitive` within its bounded scope:

- completing the `Q1` review with effective redistribution narrows or withdraws the function;
- removing the `Q2–Q4` workaround/default relation weakens the wider-path segmentation;
- treating `Q5` as full restoration produces the `Z2` no-projection route;
- fragmenting the source into unrelated Events reduces the claim to an Event Cluster or chronology;
- projecting only `Q1`, `Q4`, or `Q5` as the complete Event loses constitutive phase relations;
- replacing `W2` with `Z2` withdraws the additional Macro-Event function;
- a separately warranted narrower `Q1–Q5` source object might carry the function more precisely, but it cannot be fabricated by target-driven boundary revision.

These tests establish discrimination, not alternate-history certainty or causal proof.

## 6. Operation and Type Finding

The source Trajectory was formed by the prior `COMPOSE` occurrence in `C17-LAMBDA-01`. The current operation is a separate `PROJECT_AS` occurrence:

```text
case.c17-lambda-01.compose.01
→ source Trajectory remains intact

project-as.c40-p2.01
→ bounded Macro-Event function in W2
```

The source remains typed as `trajectory`. The target is a derived contextual function. Macro-Event is not an Event type, a PMS operator, a new primitive, or an intrinsic property of the source.

```text
COMPOSE forms the source Trajectory
PROJECT_AS assigns the target function
COMPOSE ≠ PROJECT_AS
```

## 7. Five-Part Loss

### `preserved`

- source reference, Trajectory origin type, and prior result;
- full `Q0–Q5` duration and phase order;
- central Non-Event, asymmetry, default stabilization, reversal, partial repair, and residual uncertainty;
- wider target path `W2`, comparison `Z2`, transition dimension, and defeat conditions.

### `compressed`

- non-load-bearing local actions and micro-events inside `Q0–Q5`, recoverable from the source record;
- source detail that does not alter the wider-path transition function.

Compression does not make the source instantaneous or internally homogeneous.

### `excluded`

- other paths and periodizations;
- comparison path `Z2`;
- punctual Event identity;
- complete-period identity;
- decisive or necessary turning-point status;
- causal monopoly, person typing, legitimacy, sanction, recommendation, and authority.

### `uncertain`

- exact contribution of each phase to later branching;
- exact superiority of the full `Q0–Q5` source over a separately warranted narrower source;
- quantitative phase weighting or cost transmission.

### `irrecoverable`

- a complete causal mechanism linking every source phase to every `Q6–Q8` branch condition;
- exhaustive local cost allocation not encoded in the synthetic packet.

## 8. Alternatives

Preserved alternatives are:

- a bounded Event Cluster in the wider path;
- one narrower phase as a turning Event;
- the separate `C40-P1` frame-function projection in different target coordinates;
- source Trajectory as background history only;
- structural analogy only;
- no additional projection in comparison path `Z2`;
- a future, separately warranted narrower `Q1–Q5` source object.

The current package does not merge these alternatives or revise the source boundaries to make the target claim cleaner.

## 9. Twelve-Stage Local Audit

All twelve stages are complete:

1. Source and Claim Entry — source, wider target path, transition dimension, ceilings, and exclusions declared;
2. Operation Classification — `PROJECT_AS`, separate from the prior `COMPOSE` occurrence;
3. Relevance Floor — passed through a concrete wider-path segmentation and later-branching difference;
4. Traceability Ceiling — passed through four load-bearing source traces and the `W2/Z2` contrast;
5. Continuity and Type Integrity — reference, origin type, internal duration, target function, and target frame preserved;
6. Counterfactual Sensitivity — `strongly_sensitive`;
7. Loss and Selection — exact five fields complete;
8. Alternatives — Event Cluster, narrower Event, sibling function, background, analogy, no projection, and narrower-source rival visible;
9. Source and Calibration Limits — synthetic discrimination only; no external periodization or causal calibration;
10. Anti-Immunization — the prior `COMPOSE` claim is neither repaired nor strengthened;
11. Stop and Non-Capture — no trigger for the narrowed `W2` claim;
12. Claim and Authority Ceiling — Event identity, causal reach, transfer, person typing, and authority excluded.

The stages are non-scored and non-compensatory.

## 10. Canonical Mapping

The broader candidate would treat the entire source as a decisive historical turning point across later paths. The packet supports only one transition-bearing function in `W2`, with internal duration retained and causal reach excluded.

```text
source-sensitive PROJECT_AS passage
+ preserved internal duration
+ decisive target-path and transition narrowing
+ excluded punctual and causal reach
→ admissible_with_bounded_claim
```

`admissible` is not selected because the transition, path, temporal, and causal narrowing is materially decisive. `admissible_but_provisional` is not selected because the delimited synthetic claim is supported; unresolved rival-source pressure remains visible without defeating it. Stop, Failure, Analogy-only, Partial Admissibility, and Non-Capture are not triggered for the tested claim.

## 11. Failure Preservation and Handoff

`C17-LAMBDA-01` remains an independently testable `COMPOSE` result. A later failure, narrowing, or replacement of this Macro-Event function would leave the source Trajectory intact.

`C40-P1` remains a separate compatible projection in different target coordinates:

```text
same source Trajectory
→ frame-function in C1
→ Macro-Event function in W2

separate contexts
separate functions
separate records
no forced merger
```

Contract-minimum status after this package:

```text
Chapter-40 minimum: 4/7
completed:
  C40-P1
  C40-P2
  C40-N3
  C40-X6

remaining:
  recurrent-form attractor-function
  competing projections
  person-level type-jump countercase
```

## 12. Governance

```yaml
governance:
  authority_inheritance: prohibited
```

No person, diagnosis, historically correct periodization, legitimacy, institutional decision, policy, sanction, recommendation, or application authority follows.

## 13. Artifact Links

**YAML record:** [`../yaml/C40-P2_Trajectory_as_Macro_Event.yaml`](../yaml/C40-P2_Trajectory_as_Macro_Event.yaml)  
**Case index:** [`../Case_Index.md`](../Case_Index.md)  
**Source case:** [`C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md`](C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md)  
**Sibling projection:** [`C40-P1_Trajectory_as_Bounded_Frame_Function.md`](C40-P1_Trajectory_as_Bounded_Frame_Function.md)  
**Canonical family chapter:** [`../../01_blocks/04_part_iii_retype.md#chapter-32-trajectory-as-macro-event`](../../01_blocks/04_part_iii_retype.md#chapter-32-trajectory-as-macro-event)  
**Canonical Chapter-40 packet:** [`../../01_blocks/04_part_iii_retype.md#40-3-case-2-trajectory-as-macro-event`](../../01_blocks/04_part_iii_retype.md#40-3-case-2-trajectory-as-macro-event)
