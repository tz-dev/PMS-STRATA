# C35-A1 — Operator-Weighting Profile and RETYPE Type Boundary

**Gap Package:** A  
**Chapters served:** 35 and 38  
**Families served:** `P5`, `N1`, `X3`

## 1. Purpose

This package instantiates one positive configuration-bound operator-weighting profile and two separate RETYPE boundary failures. No record changes PMS operator identity or dependency order.

## 2. Source profile W35

`W35` is reconstructed inside configuration `C35`:

```text
Ω exposure prominence
+ Θ accumulation prominence
+ all Δ–Ψ dependencies retained
→ configuration-bound operator-weighting profile W35
```

The weighting is qualitative, claim-relative, and configuration-bound.

## 3. Positive projection — C35-A1

In target continuation field `H35`, W35 changes continuation cost, option accessibility, and reopening conditions. Equalizing Ω or removing Θ accumulation weakens or defeats the function.

```text
operator-weighting profile
→ PROJECT_AS
→ bounded continuation-cost modulating function
```

**Route:** `admissible_with_bounded_claim`.

## 4. Origin-type replacement failure — C38-N1

The claim that W35 *is* Ω or a new Ω/Θ operator type replaces its origin type and alters the PMS inventory.

**Route:** `failed_transformation`.

## 5. Projection versus recontextualization — C38-X3

Changing only the interpretive frame to `R35` while holding target praxis fixed is at most Φ-recontextualization or description. It is not PROJECT_AS.

**Route:** `failed_transformation`.

## 6. Boundaries

```text
weighting ≠ operator replacement
profile ≠ formal type
profile function ≠ person trait
Φ-recontextualization ≠ PROJECT_AS
successful bounded function ≠ rescue of failed type claim
```

```yaml
governance:
  authority_inheritance: prohibited
```
