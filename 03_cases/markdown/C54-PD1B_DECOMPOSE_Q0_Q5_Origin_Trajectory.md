# C54-PD1B — Reopen Origin Trajectory After Projection

**Artifact role:** Markdown companion to one Shared Transformation Record operation occurrence  
**Operation:** `DECOMPOSE`  
**Selected Output Class:** `admissible_with_bounded_claim`  
**YAML Record:** [`C54-PD1B_DECOMPOSE_Q0_Q5_Origin_Trajectory.yaml`](../yaml/C54-PD1B_DECOMPOSE_Q0_Q5_Origin_Trajectory.yaml)  
**Package narrative:** [`C54-PD1_PROJECT_AS_to_DECOMPOSE_Origin_Source_Chain.md`](../packages/C54-PD1_PROJECT_AS_to_DECOMPOSE_Origin_Source_Chain.md)  

```text
record companion
≠ duplicate theory source
≠ independent operation occurrence
≠ replacement for the YAML record
```

## 1. Claim

After the bounded C40-P2 Macro-Event projection, the origin-typed Q0–Q5 trajectory can be reopened into initiation, missed-review, workaround, default-stabilization, and partial-repair phase clusters; the Macro-Event target function itself is not decomposed.

## 2. Source and Target

**Source object:** `trajectory.Q0-Q5` — Origin-typed workload-review trajectory previously projected in C40-P2.

**Source type:** `Trajectory`

**Target object:** `reconstruction.Q0-Q5.finer` — Finer phase-cluster reconstruction of the same origin trajectory.

## 3. Operation Occurrence

**Occurrence ID:** `decompose.c54-pd1b.01`

**Transformation context:** C54-PD1 PROJECT_AS→DECOMPOSE chain.

**Justification:** The prior PROJECT_AS result leaves the source trajectory origin-typed and available for a separate same-reference finer reconstruction.

**Expected praxeological difference:** The phase model exposes the central Non-Event and stabilization/repair relations without decomposing or validating the target function.

## 4. Local Adjudication

**Local result:** The Q0–Q5 origin trajectory is reopened into phase clusters; the target function is not decomposed.

**Canonical mapping:** `admissible_with_bounded_claim`

**Class-selection rationale:** The claim passes only with origin/function separation and prior-result preservation.

## 5. Loss Record

### `preserved`

- Q0–Q5 origin type and reference: The source remains a trajectory.
- Prior C40-P2 result and Loss: The Macro-Event function remains a separate bounded result.

### `compressed`

- Phase microdetail: Only load-bearing clusters are retained.

### `excluded`

- Target-function decomposition: The Macro-Event function is not opened as if it were a source object.

### `uncertain`

- Repair-transition mechanism: Exact route remains underdetermined.

### `irrecoverable`

- none declared

## 6. Stop and Non-Capture Boundary

**Stop reached:** `false`

**Stop rationale:** The source decomposition is adequately captured; opening the target function is a category error, not Non-Capture.

**Non-Capture asserted:** `false`

**Non-Capture rationale:** The source decomposition is adequately captured; opening the target function is a category error, not Non-Capture.

## 7. Authority Boundary

No target-function truth priority or authority follows from reopening.

```yaml
governance:
  authority_inheritance: prohibited
```

## 8. Package Relation

The full shared source narrative, chain handoffs, comparison conditions, and adjacent operation occurrences remain in [`C54-PD1_PROJECT_AS_to_DECOMPOSE_Origin_Source_Chain.md`](../packages/C54-PD1_PROJECT_AS_to_DECOMPOSE_Origin_Source_Chain.md). This companion isolates only `C54-PD1B` so that every YAML operation Record has one directly corresponding Markdown artifact.
