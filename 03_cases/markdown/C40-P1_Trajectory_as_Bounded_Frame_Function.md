# C40-P1 — Trajectory as Bounded Frame-Function

**Case class:** `positive_case`  
**Chapter owner:** Chapter 40 — RETYPE Cases, Countercases, and Local Audit  
**Operation:** `PROJECT_AS`  
**Local result:** role- and interval-bounded historical frame-function with origin type preserved  
**Canonical Output Class:** `admissible_with_bounded_claim`  
**Lock-critical:** yes — RETYPE package 1 of 3  
**Artifact status:** instantiated, schema-validated, twelve-stage audited, mapped, and indexed

## 1. Governing Claim

Within synthetic later coordination context `C1` during `Q6–Q7`, the independently reconstructed workload-review Trajectory from `C17-LAMBDA-01` performs a bounded historical frame-function for the declared overloaded-role relation `R_over`. It structures credible reopening interpretations, expected repair costs, and practically available coordination moves while remaining a Trajectory.

```text
Trajectory as origin type
+ bounded frame-function in C1
≠ Frame operator identity
≠ historical determinism
≠ authority increase
```

## 2. Source and Claim Boundary

The source is [`C17-LAMBDA-01`](C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md), an independently typed Q0–Q5 Trajectory containing a warrantable missed review, persistent workaround, asymmetric workload shift, default stabilization, partial repair, uncertainty, and irrecoverable causal-detail limits.

The target claim is limited to:

- context `C1`;
- role relation `R_over`;
- interval `Q6–Q7`;
- interpretation, expectation, repair-cost, and coordination-corridor dimensions.

It excludes all-role, permanent, deterministic, causal-monopoly, person-level, normative, policy, sanction, and application claims.

## 3. Target Packet and Comparison Context

`C1` and comparison context `Z` contain the same formal reopening rule. They differ materially:

- `C1` preserves the overloaded-role relation, residual workload binding, and incomplete repair inherited from Q0–Q5;
- `Z` contains a full role reset, released binding, and a new repair rule that carries the target reconstruction without additional historical framing.

This prevents the projection from collapsing into “history matters.” The tested difference is whether source-specific historical carriers alter practical credibility and cost for `R_over` in `C1`.

## 4. Tested Projection

### Load-bearing source traces

1. **Missed review and repeated non-repair** condition expectations that nominal reopening may again fail to redistribute workload.
2. **Persistent workaround, asymmetric load, and default stabilization** condition which coordination moves appear practically credible or costly for `R_over`.
3. **Partial repair** reduces but does not erase accumulated revision and exit costs.

### Target-side praxis difference

The projection distinguishes:

```text
formally available reopening option
≠ practically credible and equally costly option
```

for the declared role relation in `C1`.

## 5. Counterfactual Frame Test

The claim is `strongly_sensitive` within its bounded scope:

- timely credible review and redistribution narrows or withdraws the expectation component;
- release of residual binding weakens or defeats the role-specific function;
- complete repair and role reset produce no additional projection in `Z`;
- replacing `R_over` with an unaffected role relation defeats transfer;
- removing the target-context difference reduces the source to historical background.

These are discriminative tests, not causal proof or alternate-history reconstruction.

## 6. Operation and Type Finding

The operation is `PROJECT_AS`, not `COMPOSE`, `DECOMPOSE`, recontextualization, analogy, or label substitution.

```text
composite.c17-lambda-01
PROJECT_AS
bounded historical frame-function in C1
```

The source remains typed as `trajectory`. The target function is a derived contextual function and does not become `□`, a Frame operator occurrence, or a new PMS primitive.

## 7. Five-Part Loss

### `preserved`

- source reference and Trajectory origin type;
- Q0–Q5 duration, central Non-Event, residual binding, default stabilization, and partial repair;
- target context `C1`, role relation `R_over`, interval `Q6–Q7`;
- prior source disposition, uncertainty, and counterfactual defeat conditions.

### `compressed`

- non-load-bearing quarterly micro-events, recoverable from the source record;
- source-internal detail that does not alter the bounded target function.

### `excluded`

- other roles, periods, and contexts;
- context `Z`, where no additional projection is warranted;
- historical determinism, causal monopoly, Frame identity, person typing, legitimacy, sanction, recommendation, and authority.

### `uncertain`

- exact magnitude and allocation of accumulated revision and exit costs.

### `irrecoverable`

- a complete causal mechanism connecting every source phase to every later interpretation.

## 8. Alternatives

Preserved alternatives are:

- bounded Path-Dependence or historical-load description without target function;
- historical background relevance only;
- one narrower expectation effect;
- structural analogy only;
- no transformation in comparison context `Z`.

The positive C1 route does not invalidate these alternatives globally.

## 9. Twelve-Stage Local Audit

All twelve stages are complete:

1. Source and Claim Entry — complete;
2. Operation Classification — `PROJECT_AS`;
3. Relevance Floor — passed through role-specific praxis difference;
4. Traceability Ceiling — passed through three load-bearing traces;
5. Continuity and Type Integrity — source, type, function, time, and context separated;
6. Counterfactual Sensitivity — `strongly_sensitive`;
7. Loss and Selection — exact five fields complete;
8. Alternatives — rival, narrower, analogy, and no-transformation options visible;
9. Source and Calibration Limits — synthetic discrimination only, no external calibration;
10. Anti-Immunization — prior COMPOSE claim neither repaired nor strengthened;
11. Stop and Non-Capture — no trigger for the narrowed claim;
12. Claim and Authority Ceiling — passed under explicit exclusions.

The stages are non-scored and non-compensatory.

## 10. Canonical Mapping

The broad candidate would frame every role and later interval. The packet supports only `R_over` in `C1` during `Q6–Q7`.

```text
source-sensitive PROJECT_AS passage
+ decisive role/context/time narrowing
→ admissible_with_bounded_claim
```

`admissible` is not selected because the narrowing is materially decisive. `admissible_but_provisional` is not selected because the delimited synthetic packet supplies the required support; uncertainty is retained inside the bounded claim. Stop, Failure, Analogy-only, and Non-Capture are not triggered for this claim.

## 11. Failure Preservation and Handoff

`C17-LAMBDA-01` remains an independently testable `COMPOSE` result. This projection neither repairs nor strengthens it. A later failure of the frame-function would leave the source Trajectory intact.

```text
prior COMPOSE result
≠ current PROJECT_AS result

current PROJECT_AS success
≠ RETYPE lock complete
```

Two lock-critical packages remain: label-substitution failure and projection-versus-analogy confusion.

## 12. Governance

```yaml
governance:
  authority_inheritance: prohibited
```

No person, diagnosis, legitimacy, coordination decision, policy, sanction, recommendation, or application authority follows.

## 13. Artifact Links

**YAML record:** [`../yaml/C40-P1_Trajectory_as_Bounded_Frame_Function.yaml`](../yaml/C40-P1_Trajectory_as_Bounded_Frame_Function.yaml)  
**Case index:** [`../Case_Index.md`](../Case_Index.md)  
**Source case:** [`C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md`](C17-LAMBDA-01_Trajectory_with_Central_Non_Event.md)  
**Canonical RETYPE chapter:** [`../../01_blocks/04_part_iii_retype.md#40-2-case-1-trajectory-as-frame-function`](../../01_blocks/04_part_iii_retype.md#40-2-case-1-trajectory-as-frame-function)
