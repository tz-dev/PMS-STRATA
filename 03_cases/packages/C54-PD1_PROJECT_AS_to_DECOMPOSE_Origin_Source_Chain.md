# C54-PD1 — PROJECT_AS → DECOMPOSE Origin-Source Chain

**Chain:** `PROJECT_AS → DECOMPOSE`  
**Existing first record:** `C40-P2`  
**New second record:** `C54-PD1B`  
**Integrated result:** `admissible_with_bounded_claim`

## 1. PROJECT_AS

`C40-P2` projects the origin-typed `Q0–Q5` trajectory as a bounded Macro-Event function in wider path `W2`.

## 2. DECOMPOSE

The second occurrence reopens the **origin trajectory**, not the target function:

```text
initiation
→ missed review
→ workaround persistence
→ default stabilization
→ partial repair
```

```text
PROJECT_AS target function
≠ decomposable source container
```

## 3. Preservation

The C40-P2 projection result, its Loss, and its claim ceiling remain preserved. The finer source model neither proves nor erases that projection.

```yaml
governance:
  authority_inheritance: prohibited
```
