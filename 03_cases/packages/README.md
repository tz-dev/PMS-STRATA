# Shared Case and Chain Narratives

This directory contains **10 multi-record package narratives plus this README**.

```text
directory files:      11
package narratives:   10
README:                 1
```

A package narrative exists only where several distinct operation occurrences require a shared source context, controlled comparison, continuity account, or chain handoff. It is therefore **not** organized as one file per chapter.

Current package families:

- one operator-weighting/type-boundary package;
- one competing-projection package;
- one COMPOSE → PROJECT_AS continuity package;
- one calibration/Stop/re-entry package;
- one failure-preservation/claim-rescue package;
- five integrated chain packages.

```text
package narrative
≠ operation record
≠ merged local result
≠ chapter coverage unit
≠ authority source
```

Each operation occurrence represented by a package retains its exact YAML/Markdown pair in `../yaml/` and `../markdown/`. Chapters and cases without a genuine multi-record relation require no package narrative.
