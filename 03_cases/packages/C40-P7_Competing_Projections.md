# C40-P7 — Two Competing Projections in Shared Target Coordinates

**Case class:** `positive_case / competing-projection case`  
**Chapter owner:** Chapter 40 — RETYPE Cases, Countercases, and Local Audit  
**Primary family owner:** Chapter 36 — Competing Projections  
**Operations:** two separately recorded `PROJECT_AS` occurrences  
**Shared source object:** derived recurrent-form composite `R7`  
**Shared target coordinates:** continuation field `C7`, configuration level, C7-R0–R4  
**Candidate F:** bounded historical `frame-function`  
**Candidate G:** bounded dynamic `attractor-function`  
**Canonical Output Class of each candidate:** `admissible_but_provisional`  
**Comparison result:** `provisional dual support; no settled preference`  
**Lock-critical:** no  
**YAML records:** [`C40-P7F`](../yaml/C40-P7F_Frame_Function_Candidate.yaml) · [`C40-P7G`](../yaml/C40-P7G_Attractor_Function_Candidate.yaml)  
**Canonical family section:** [`Chapter 40.8`](../../01_blocks/04_part_iii_retype.md#40-8-case-7-two-competing-projections)

---

## 1. Case Statement

The same origin-typed recurrent-form source `R7` is projected into the same target object, level, frame, granularity, temporal window, claim scope, and validity scope through two distinct candidate functions.

```text
R7 → PROJECT_AS → F: bounded historical frame-function
R7 → PROJECT_AS → G: bounded dynamic attractor-function
```

The two arrows are **two operation occurrences** and therefore have separate records. Their comparison is an audit relation, not a fourth STRATA operation.

Both candidates perform genuine target work:

- `F` narrows the credible interpretation set, role expectations, and revision-cost assumptions in `C7`;
- `G` changes transition accessibility, departure friction, role/resource persistence, and reopening cost in `C7`.

They compete because these effects materially overlap in the same target coordinates. The current synthetic fixture does not independently isolate how much of the common expectation/access/persistence difference belongs to `F` rather than `G`.

```text
same source + same target coordinates + overlapping praxis work
→ genuine competition

competition
≠ forced merger
≠ one true source type
≠ numerical ranking
```

## 2. Shared Source and Target Boundary

`R7` retains the same bounded recurrent-form warrant used in `C40-P3`: provisional continuation, incomplete reset, role/resource lock-in, and constrained reopening recur across `C17-LAMBDA-01`, `C17-ATTR-01`, and `C17-PROJ-01` under declared comparison coordinates.

The shared target packet `C7` contains:

- an established provisional-route template;
- sedimented expectations that nominal review will not fully reset roles;
- reserved resources and inherited commitments favouring continuation;
- higher release and redesign friction than partial reopening;
- one bounded C7-R0–R4 continuation window.

The target coordinates remain identical across both records. Apparent competition cannot be dissolved by silently changing context, level, frame, granularity, role set, or temporal scope.

## 3. Candidate F — Historical Frame-Function

Candidate `F` claims that `R7` structures which interpretations and coordination moves remain credible in `C7`.

Load-bearing traces:

- repeated survival of temporary arrangements through nominal review;
- sedimented expectation that reopening remains partial;
- accumulated interpretive and practical revision cost;
- role expectations carried into the target window.

The claim fails or narrows if those expectations disappear while target transition friction remains unchanged.

## 4. Candidate G — Dynamic Attractor-Function

Candidate `G` claims that `R7` changes the accessibility and persistence of the familiar transition form in `C7`.

Load-bearing traces:

- recurrent provisional continuation and incomplete reset;
- role/resource lock-in;
- asymmetric departure and release friction;
- constrained reopening rather than clean reset.

The claim fails or narrows if route access and departure friction are equalized while target interpretation remains unchanged.

## 5. Why No Preference Is Selected

The available packet contains a matched no-uptake rival in which both historical expectation and transition friction disappear. It therefore shows that source uptake matters, but not which candidate supplies the comparatively better explanation.

The decisive selective tests remain unavailable:

1. vary sedimented expectation while holding transition friction stable;
2. vary transition friction while holding sedimented expectation stable;
3. test whether one candidate recovers the other candidate’s practical distinctions without importing its relations;
4. retest source-only and no-projection in exactly the same coordinates.

Because the current fixture covaries the candidates’ load, a bounded preference would exceed the available trace.

```text
both candidates coherent and usable
+ material rival stability unresolved
→ admissible_but_provisional
```

This is not `partially_admissible`: neither candidate is split into supported and unsupported components. It is not `non_capture`: each candidate remains adequately reconstructible. It is not `failed_transformation`: both pass their own family conditions. The unresolved issue is comparative stability.

## 6. Five-Part Loss

```yaml
loss:
  preserved:
    - shared source identity and origin type
    - identical target coordinates
    - candidate-specific source traces and target work
    - serious rival, source-only, and no-projection options
  compressed:
    - source micro-events not needed for candidate comparison
    - target micro-negotiations outside expectation/access/persistence
  excluded:
    - hybrid super-function
    - true-type claim
    - numerical or compensatory ranking
    - person, theory, political, or authority tribunal
  uncertain:
    - independent contribution of F and G
    - comparative parsimony under selective mutations
    - stability outside the synthetic C7 packet
  irrecoverable:
    - unrecorded microstates and quantitative contribution weights
    - empirical causal or probabilistic evidence not present in the sources
```

## 7. Alternatives and Counterpressure

Live outcomes remain:

- bounded preference for `F` under new discriminating support;
- bounded preference for `G` under new discriminating support;
- continued provisional dual support;
- narrower role- or phase-specific segmentation under separate records;
- source-only or no-projection;
- genuine `non_capture` if no responsible comparative or candidate-level route survives.

A hybrid label is prohibited unless independently claimed and tested through a new `PROJECT_AS` record. The most comprehensive description is not automatically superior.

## 8. Local Audit Result

Both records complete the twelve-stage audit, preserve exact five-part Loss, retain serious alternatives, and maintain Type Integrity, Reference and Functional Continuity, Contextual Boundedness, Counterfactual Sensitivity, Source and Claim Ceilings, Stop, Non-Capture, and non-authority.

**Local audit result:** `pass_with_material_provisionality`  
**Candidate F:** `admissible_but_provisional`  
**Candidate G:** `admissible_but_provisional`  
**Comparative preference:** not selected  
**Forced integration:** prohibited  
**Authority inheritance:** prohibited

## 9. Re-entry Boundary

Any resolved preference requires new candidate-specific evidence or a new target fixture and therefore a new record. The current provisional results remain recorded and are not erased by later preference, segmentation, failure, Stop, or Non-Capture.

---

**Case index:** [`../Case_Index.md`](../Case_Index.md)  
**Authority:** case artifact below canonical prose; no independent theory, empirical, causal, normative, person-level, political, legal, governance, or application authority.
