# Case YAML Operation Records

This directory contains 59 Shared Transformation Records, one per operation occurrence. Every YAML file has an exact same-basename Markdown companion in [`../markdown/`](../markdown/).

Schema validity does not decide substantive admissibility, causality, legitimacy, or authority.
