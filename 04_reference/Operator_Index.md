# PMS-STRATA — Operator Index

**Status:** Reference Kernel v0.1.32 — Chapter-10-WP2-synchronized PMS-operator control  
**Repository role:** `04_reference` navigation and operator-boundary control; not an independent PMS or STRATA theory source
**Control sources:** `PMS.yaml` for the repository-reference operator inventory, names, order, dependencies, layers, and Base guardrails; *Towards a Praxeological Meta-Structure Theory* as a supporting PMS explanatory source; [`Chapter 1`](../01_blocks/01_foundations.md#chapter-1-object-model-operator-type-operator-occurrence-and-composite-structure) for the provisionally locked object model and identity burden; `05_minified/*` and `05_minified/Chapter_Contracts.md` for the limits of STRATA inference, transformation, and claim; [`Chapter 2`](../01_blocks/01_foundations.md#chapter-2-frame-granularity-and-relative-level) for analytical-coordinate boundaries; [`Chapter 3`](../01_blocks/01_foundations.md#chapter-3-configuration-event-non-event-transition-path-and-trajectory) for temporal-object burdens; [`Chapter 4`](../01_blocks/01_foundations.md#chapter-4-the-three-strata-operations-compose-decompose-and-project_as) for the closed three-operation grammar; and `04_reference/Chapter_1_Preparation_Record.md` through `04_reference/Chapter_5_Preparation_Record.md` for non-theory production status and handoff

---

## 1. Function and Authority

This index records how the eleven PMS Base operator types Δ–Ψ may be referenced within PMS-STRATA without being renamed, reordered, decomposed, extended, retyped, or converted into an ontology of strata. It is a control and navigation artifact. It does not replace `PMS.yaml`, the PMS paper, the current canonical Foundations prose, or the Formal Model Core.

The authority rule is:

```text
PMS.yaml
→ canonical repository-reference inventory, names, order, dependencies, and base guardrails

PMS paper
→ supporting explanatory elaboration, formal role, generative reasoning, and examples

PMS-STRATA
→ controls only what STRATA may infer, transform, or claim through COMPOSE, DECOMPOSE, and PROJECT_AS
```

`PMS.yaml` controls the current Base inventory, canonical signs and names, order, direct dependencies, organizational layers, and repository guardrails. The PMS paper supplies explanatory elaboration. The STRATA claim boundary controls only what STRATA may infer, transform, or claim from those sources; it does not revise PMS Base. Neither source makes the operator inventory complete, uniquely optimal, ontologically final, empirically self-validating, or immune to revision.

---

## 2. Canonical Distinctions

### 2.1 Sign, name, type, occurrence, and composite

```text
operator sign
≠
operator name
≠
operator type
≠
operator occurrence
≠
composite structure
```

- **Operator sign** is the canonical symbol used in the current PMS repository reference, such as `□`.
- **Operator name** is the canonical English label associated with that sign and type, such as `Frame`.
- **Operator type** is the abstract PMS Base function to which the sign and name refer.
- **Operator occurrence** is a concrete, reference-bound, context- or frame-bound, source-bound, and claim-bound structure reconstructed as expressing an operator function in a specific analysis.
- **Composite structure** is a selectively formed analytical object containing multiple identifiable constituents whose declared relations are constitutive of the object claimed.

[Chapter 1 WP1](../01_blocks/01_foundations.md#chapter-1-object-model-operator-type-operator-occurrence-and-composite-structure) is the primary STRATA definition site for these distinctions. The meanings, inventory, order, and dependencies of the operator types remain governed by PMS Base.

### 2.2 Meta-axiom language

The PMS paper calls Δ–Ψ **meta-axioms** and **meta-operators**. In this index, `PMS Base operator type` is the controlled term. Each type is treated as non-derivative at its own level of description within the current PMS reference. `Meta-axiom` does not mean an empirically proven law, an ontological constituent, or a proposition beyond revision.

### 2.3 Four distinct senses of composition

| Term | Meaning | Status |
|---|---|---|
| PMS dependency path | Default repository-reference relation among operator types | Base grammar |
| PMS operator composition | Formal or generative combination of operator types, often written with `∘` | Base-theory derivational representation |
| `Σ` Integration | One PMS operator type coordinating disparate or conflicting structures | Base operator |
| `COMPOSE` | STRATA operation forming a declared composite analytical object from multiple source structures | STRATA method |

These senses must not be substituted for one another.

```text
PMS operator composition
≠
Σ Integration
≠
STRATA COMPOSE
```

### 2.4 Operator-like target functions

A source object may, under `PROJECT_AS`, perform a bounded contextual function that resembles or corresponds to an operator function. The origin type remains unchanged.

```text
trajectory PROJECT_AS frame-function
≠
trajectory becomes □
```

The same rule applies to attractor-functions, asymmetry-functions, binding-functions, integration-functions, boundary functions, and other bounded higher-level functions.

---

## 3. Canonical Registry

| Order | Sign | Canonical name | German working rendering | Canonical dependencies | PMS status |
|---:|:---:|---|---|---|---|
| 1 | `Δ` | Difference | Differenz | — | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 2 | `∇` | Impulse | Impuls / Gradient | `Δ` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 3 | `□` | Frame | Frame / Rahmung | `Δ`, `∇` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 4 | `Λ` | Non-Event | Nicht-Ereignis | `□` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 5 | `Α` | Attractor | Attraktor | `Δ`, `∇`, `□`, `Λ` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 6 | `Ω` | Asymmetry | Asymmetrie | `Α` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 7 | `Θ` | Temporality | Temporalität | `Ω`, `Α` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 8 | `Φ` | Recontextualization | Rekontextualisierung | `Θ`, `Ω`, `□` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 9 | `Χ` | Distance | Distanz | `Φ`, `Θ`, `□` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 10 | `Σ` | Integration | Integration | `Χ`, `Φ` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |
| 11 | `Ψ` | Self-Binding | Selbstbindung | `Σ`, `Θ`, `Χ` | treated as structurally basic and non-derivative at its own level of description within the current PMS reference |

The order records one systematically motivated dependency path in the current repository reference. It is binding for PMS-conformant repository work unless a versioned PMS revision states otherwise. It is not an ontology, a rank order of value, a developmental ladder, or proof of unique formal optimality.

---

## 4. Dependency Path and Operator Layers

### 4.1 Direct dependency path

```text
Δ
→ ∇
→ □
→ Λ
→ Α
→ Ω
→ Θ
→ Φ
→ Χ
→ Σ
→ Ψ
```

This linear display is a reading aid, not a complete dependency graph. The canonical direct dependencies are:

- `Δ` Difference: none
- `∇` Impulse: `Δ`
- `□` Frame: `Δ`, `∇`
- `Λ` Non-Event: `□`
- `Α` Attractor: `Δ`, `∇`, `□`, `Λ`
- `Ω` Asymmetry: `Α`
- `Θ` Temporality: `Ω`, `Α`
- `Φ` Recontextualization: `Θ`, `Ω`, `□`
- `Χ` Distance: `Φ`, `Θ`, `□`
- `Σ` Integration: `Χ`, `Φ`
- `Ψ` Self-Binding: `Σ`, `Θ`, `Χ`

### 4.2 Organizational operator layers

`PMS.yaml` additionally groups the operator types into four organizational layers:

| Layer | Canonical name | Contains | Controlled interpretation |
|---|---|---|---|
| `L1` | Early structural patterning | `Δ`, `∇`, `□`, `Λ`, `Α` | distinctions, impulses, frames, non-events, and recurrent patterns |
| `L2` | Relational asymmetry and temporality | `Ω`, `Θ` | asymmetrical relations and temporal extension |
| `L3` | Meta-structural reflexivity | `Φ`, `Χ`, `Σ` | transformation, distance, and integration |
| `L4` | Higher-order self-binding | `Ψ` | relatively stable self-binding across time and contexts |

These layers organize the current PMS reference. They are not STRATA Parts, relative levels, granularities, ontological strata, developmental ranks, or authority ranks.

```text
PMS operator layers L1–L4
≠
PATH–SUB–RETYPE–LIMITS
```

Three order representations must therefore remain distinct:

```text
direct dependency graph
≠
linear reading path
≠
organizational operator layers
```

STRATA must not infer from the dependency path or the organizational layers that:

- later operators possess greater truth or authority;
- earlier operators are lower ontological layers;
- every occurrence must instantiate the full chain;
- the path is the only possible future PMS formalization;
- a derived structure inherits the order position of the operator it resembles;
- an operator may be omitted from `PMS.yaml` dependencies because a local STRATA case appears to work without it.

---

## 5. Detailed Operator Entries

### 5.1 `Δ` — Difference

- **Canonical sign:** `Δ`
- **Canonical name:** `Difference`
- **German working rendering:** Differenz
- **Repository order:** 1 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Minimal structural distinction enabling differentiation.
- **Canonical dependencies:** None in the current repository-reference dependency path.
- **Structures provided in `PMS.yaml`:** `boundary_formation`, `object_differentiation`, `structural_contrast`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Δ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.1 and §10.3

### Formal role

Marks or constructs a distinction through which an object, boundary, contrast, or role difference becomes structurally legible.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a locally relevant inside/outside distinction
- a role difference made visible within a scene
- a contrast between available and unavailable action corridors

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Differences help mark changed configurations and branch distinctions, but a difference alone is not a transition, path, or trajectory.
- **SUB:** A concrete difference-typed occurrence may be opened into the practices and relations that maintain the distinction; Δ itself is not decomposed.
- **RETYPE:** An origin-typed object may perform a bounded differentiating or boundary function only under an explicit target context; it does not become Δ.
- **LIMITS:** Additional differences must cross the Praxeological Relevance Floor and remain source-supported.

### Central non-equivalences

- Difference operator type ≠ any particular empirical difference
- difference ≠ asymmetry
- difference ≠ praxeological relevance
- additional distinction ≠ additional finding

### Must not be claimed

- that reality is ontologically composed of differences as discrete strata
- that every analyst-added distinction is praxeologically significant

### Paper-to-STRATA calibration note

The paper treats Δ as first in the proposed dependency sequence and structurally basic within PMS, while keeping the completeness and unique optimality of the full inventory open.

---

### 5.2 `∇` — Impulse

- **Canonical sign:** `∇`
- **Canonical name:** `Impulse`
- **German working rendering:** Impuls / Gradient
- **Repository order:** 2 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Directional tension or drive arising from difference.
- **Canonical dependencies:** `Δ` Difference
- **Structures provided in `PMS.yaml`:** `activation`, `orientation`, `energetic_gradient`, `proto_agency`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="∇"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.2 and §10.3

### Formal role

Introduces directed activation, tension, or gradient after a difference has been established.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a directional response to a perceived difference
- an activation gradient toward or away from a configuration
- a context-bound tension that orients possible action

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Impulse occurrences may orient transitions, but directional activation alone does not establish temporal order, causality, or path dependence.
- **SUB:** A concrete impulse-typed occurrence may be reconstructed through its activating differences, frames, delays, or reinforcing relations.
- **RETYPE:** A source object may modulate directional activation in a target context, but an impulse-like function is not a new ∇ operator type.
- **LIMITS:** STRATA may not infer hidden motivation or psychological drive from a merely observed sequence.

### Central non-equivalences

- Impulse operator type ≠ inferred inner motive
- directional activation ≠ intention
- impulse ≠ action
- impulse occurrence ≠ causal proof

### Must not be claimed

- that a concrete occurrence reveals a hidden mental drive or intention
- that directionality alone establishes agency, responsibility, or causality

### Paper-to-STRATA calibration note

The paper describes ∇ as directional tension following difference; this is a formal role, not a license to infer subjective motivation.

---

### 5.3 `□` — Frame

- **Canonical sign:** `□`
- **Canonical name:** `Frame`
- **German working rendering:** Frame / Rahmung
- **Repository order:** 3 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Contextual structure that constrains and shapes impulses.
- **Canonical dependencies:** `Δ` Difference, `∇` Impulse
- **Structures provided in `PMS.yaml`:** `context`, `boundary_conditions`, `relevance_structuring`, `role_space`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="□"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.3 and §10.3
  - canonical STRATA object distinction and minimal occurrence case: [`Chapter 1 WP1`](../01_blocks/01_foundations.md#chapter-1-object-model-operator-type-operator-occurrence-and-composite-structure)

### Formal role

Stabilizes a contextual field that selects relevance and constrains, enables, or channels possible praxis.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- an institutional rule-set structuring relevance
- a conversational or relational frame
- a local boundary that determines what counts as inside, outside, expected, or actionable

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Frames bound which configurations, transitions, expectations, and non-events belong to a path reconstruction.
- **SUB:** A frame-typed occurrence may be opened into selection, exclusion, access, routine, and role practices that reproduce it.
- **RETYPE:** A trajectory or composite may be projected as a bounded frame-function while retaining its origin type.
- **LIMITS:** Frame change must remain distinct from granularity change, relative-level change, transformation context, and authority change.

### Central non-equivalences

- Frame operator type ≠ a concrete frame occurrence
- frame ≠ granularity
- frame ≠ relative level
- frame ≠ transformation context
- frame-function ≠ Frame operator identity

### Must not be claimed

- that a frame is a universal or objective layer of reality
- that a projected frame-function changes the source object into □

### Paper-to-STRATA calibration note

The paper uses frame broadly for contextual containment. STRATA must further separate frame from granularity, relative level, and transformation context.

---

### 5.4 `Λ` — Non-Event

- **Canonical sign:** `Λ`
- **Canonical name:** `Non-Event`
- **German working rendering:** Nicht-Ereignis
- **Repository order:** 4 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Structured absence: meaningful failure or delay of an expected occurrence within a frame.
- **Canonical dependencies:** `□` Frame
- **Structures provided in `PMS.yaml`:** `expectation`, `counterfactual_structure`, `tension_through_absence`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Λ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.4 and §10.3
  - canonical STRATA non-event-structure object burden: [`Chapter 1 WP2`](../01_blocks/01_foundations.md#chapter-1-object-model-operator-type-operator-occurrence-and-composite-structure)

### Formal role

Marks the meaningful non-realization, delay, suspension, or failure of an expected occurrence inside a declared frame.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a promised response that fails to occur inside its expected window
- a delayed or repeatedly deferred decision
- an expected repair, exit, or transition that remains unrealized

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Non-events may carry temporal and trajectorial load when an expectation frame and relevant non-realization are established.
- **SUB:** A structured non-event may be opened into delays, blockages, distributed non-decisions, or missing repair while preserving its non-event character.
- **RETYPE:** Repeated non-events may support a higher-level function only through explicit composition and projection records.
- **LIMITS:** Missing information, an unknown event, or an absent record is not a Λ occurrence. Chapter 1 WP2 additionally requires an identifiable expected occurrence, warranted expectation relation, bounded realization condition, source-supported non-realization, and praxeological load before a non-event structure may be claimed.

### Central non-equivalences

- Non-Event operator type ≠ every absence
- non-event ≠ missing information
- non-event ≠ unknown event
- non-event ≠ active distance
- positive sub-events ≠ elimination of the non-event character

### Must not be claimed

- that every omission, gap, silence, or absent record is a Non-Event occurrence
- that the expectation structure may be invented retrospectively without source support

### Paper-to-STRATA calibration note

The paper defines Λ through meaningful expected non-occurrence inside a frame. STRATA adds stricter source, expectation, and missing-information controls.

---

### 5.5 `Α` — Attractor

- **Canonical sign:** `Α`
- **Canonical name:** `Attractor`
- **German working rendering:** Attraktor
- **Repository order:** 5 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Recurrent pattern or stabilization emerging from repeated structural interaction.
- **Canonical dependencies:** `Δ` Difference, `∇` Impulse, `□` Frame, `Λ` Non-Event
- **Structures provided in `PMS.yaml`:** `pattern_formation`, `stability`, `path_dependence`, `role_script_emergence`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Α"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.5 and §10.3

### Formal role

Stabilizes recurrent structural interaction into a pattern that biases later configurations or transitions.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a recurrent avoidance, escalation, repair, or coordination pattern
- a stabilized institutional routine
- a dynamic transition form that becomes easier to reproduce

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Attractor occurrences may help explain recurrence and sedimentation, but they do not by themselves prove a trajectory or path dependence.
- **SUB:** An attractor-typed occurrence may be opened into repetition, expectation, cost, role, non-event, and exit structures that reproduce it.
- **RETYPE:** A recurrent trajectory form may be projected as an attractor-function when repetition, source trace, and counterfactual sensitivity are established.
- **LIMITS:** A recurring label without a stabilizing mechanism is not a traceable attractor claim.

### Central non-equivalences

- Attractor operator type ≠ a recurring label
- recurrence ≠ attractor
- attractor occurrence ≠ trajectory
- attractor occurrence ≠ path dependence
- attractor-function ≠ Attractor operator identity

### Must not be claimed

- that repetition alone proves an attractor
- that an attractor determines all future action or establishes path dependence by itself

### Paper-to-STRATA calibration note

The paper lists path dependence among the structures enabled by attractor formation. STRATA treats path dependence as a separate claim requiring temporal and counterfactual support.

---

### 5.6 `Ω` — Asymmetry

- **Canonical sign:** `Ω`
- **Canonical name:** `Asymmetry`
- **German working rendering:** Asymmetrie
- **Repository order:** 6 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Structural imbalance establishing directionality of power, exposure, capacity, or obligation.
- **Canonical dependencies:** `Α` Attractor
- **Structures provided in `PMS.yaml`:** `role_differentiation`, `responsibility_gradients`, `vulnerability_structures`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Ω"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.6 and §10.3

### Formal role

Structures directional inequality in capacity, exposure, power, burden, access, dependency, or obligation.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- unequal access to resources, information, time, or exit
- a role-specific burden or responsibility gradient
- asymmetrical exposure, authority, or dependency inside a configuration

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Asymmetries can accumulate, distribute costs, alter branch availability, and sediment over time.
- **SUB:** A concrete asymmetry may be opened into local differences in access, exposure, resources, roles, time, and exit conditions.
- **RETYPE:** Distributed local asymmetries may support a bounded higher-level asymmetry-function without becoming a new Ω type.
- **LIMITS:** Structural asymmetry is not moral judgment, person ranking, diagnosis, guilt attribution, or automatic responsibility verdict.

### Central non-equivalences

- Asymmetry operator type ≠ any simple difference
- structural asymmetry ≠ moral hierarchy
- asymmetry ≠ person essence
- local asymmetry ≠ higher-level asymmetry-function without a separate projection

### Must not be claimed

- that asymmetry automatically determines blame, legitimacy, moral rank, or sanction
- that an Ω-typed scene authorizes a global person label

### Paper-to-STRATA calibration note

The paper gives Ω a strong role in responsibility and vulnerability. STRATA retains structural asymmetry but prohibits automatic moral, legal, or person-level conclusions.

---

### 5.7 `Θ` — Temporality

- **Canonical sign:** `Θ`
- **Canonical name:** `Temporality`
- **German working rendering:** Temporalität
- **Repository order:** 7 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Temporal structuring that enables trajectories, commitments, development, and longer-form self-relation.
- **Canonical dependencies:** `Ω` Asymmetry, `Α` Attractor
- **Structures provided in `PMS.yaml`:** `sequence_and_trajectory`, `persistence`, `anticipation_and_delay`, `developmental_arcs`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Θ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.7 and §10.3

### Formal role

Orders and extends structures through duration, sequence, persistence, anticipation, delay, and development.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- duration and order among events, non-events, and transitions
- persistence or delay that changes later possibilities
- temporal extension of commitments, costs, and structural effects

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Θ supplies temporal ordering and the structural capacity for sequence. A concrete STRATA sequence claim still requires ordered source elements, a declared frame, and a bounded reconstruction. Θ alone does not establish path, trajectory, or path dependence.
- **SUB:** Temporal structures may be opened under finer resolution while preserving internal order and duration.
- **RETYPE:** A temporally extended source may perform a macro-event or frame-function only through a separate PROJECT_AS claim.
- **LIMITS:** Temporal extension must not be converted into teleology, inevitability, absolute irreversibility, or causal proof.

### Central non-equivalences

- Temporality operator type ≠ chronological list
- temporality operator type ≠ concrete sequence or established STRATA sequence claim
- temporality ≠ path
- temporality ≠ trajectory
- temporality ≠ path dependence
- duration ≠ historical necessity

### Must not be claimed

- that temporal sequence proves causality, teleology, development, or irreversible necessity
- that Θ alone establishes a STRATA sequence, path, trajectory, or path-dependence claim

### Paper-to-STRATA calibration note

The paper links Θ to sequence, trajectory, and development. STRATA preserves temporal ordering and sequence-capacity as Θ contributions while requiring ordered source elements and a declared reconstruction for a concrete sequence claim. Path, trajectory, and path-dependence claims require additional COMPOSE structure and admissibility tests.

---

### 5.8 `Φ` — Recontextualization

- **Canonical sign:** `Φ`
- **Canonical name:** `Recontextualization`
- **German working rendering:** Rekontextualisierung
- **Repository order:** 8 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Transformation through placing an existing structure into a new frame.
- **Canonical dependencies:** `Θ` Temporality, `Ω` Asymmetry, `□` Frame
- **Structures provided in `PMS.yaml`:** `adaptation`, `reinterpretation`, `developmental_change`, `pattern_shift`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Φ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.8 and §10.3

### Formal role

Places an existing structure into a changed frame so that its meaning, role, or relevance is transformed.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a conflict re-read under a changed interpretive frame
- a role or event assigned new relevance after later developments
- a contextual shift that changes the function or meaning of an existing structure

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Later recontextualization may alter the present reading of earlier events without erasing their historical position.
- **SUB:** A changed frame during decomposition must be declared and distinguished from a change of granularity.
- **RETYPE:** Recontextualization changes the frame of interpretation; PROJECT_AS additionally asserts a bounded target function. They are not equivalent.
- **LIMITS:** A new frame cannot be used to erase an earlier failed claim or to immunize a reconstruction.

### Central non-equivalences

- Recontextualization operator type ≠ every change of wording
- changed frame ≠ PROJECT_AS
- recontextualization ≠ origin-type replacement
- new interpretation ≠ erasure of source history

### Must not be claimed

- that reinterpretation removes earlier failure, cost, or historical load
- that every frame change is a STRATA operation

### Paper-to-STRATA calibration note

The paper treats Φ as a meta-transformational operator. STRATA preserves this PMS meaning while keeping Φ distinct from PROJECT_AS.

---

### 5.9 `Χ` — Distance

- **Canonical sign:** `Χ`
- **Canonical name:** `Distance`
- **German working rendering:** Distanz
- **Repository order:** 9 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Reflective withdrawal that attenuates immediate impulses and patterns.
- **Canonical dependencies:** `Φ` Recontextualization, `Θ` Temporality, `□` Frame
- **Structures provided in `PMS.yaml`:** `regulation`, `inhibition`, `reflection_and_meta_position`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Χ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.9 and §10.3

### Formal role

Creates reflective separation from immediate impulse, framing, or recurrent pattern and thereby permits inhibition and evaluation.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a pause that suspends immediate enactment
- a reflective step outside a currently dominant role or frame
- an active reduction of the immediate force of an attractor or impulse

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** A distance-typed occurrence may contribute to interruption, deferral, or redirection within a path reconstruction, but it does not reset the path history.
- **SUB:** A distance-typed occurrence may be reconstructed through pauses, inhibition, separation, and changed access to immediate enactment.
- **RETYPE:** A structure may perform a modulating or restraint function under bounded conditions without becoming Χ.
- **LIMITS:** Distance is active separation, not mere absence, non-event, withdrawal diagnosis, or proof of maturity.

### Central non-equivalences

- Distance operator type ≠ Λ
- distance ≠ passivity
- distance ≠ disappearance of relation
- distance occurrence ≠ maturity diagnosis

### Must not be claimed

- that distance is always normatively superior or developmentally mature
- that withholding action is necessarily reflective distance rather than delay, incapacity, or non-event

### Paper-to-STRATA calibration note

The paper links Χ to reflexivity and mature praxis. STRATA does not use the presence or absence of Χ to diagnose or rank persons.

---

### 5.10 `Σ` — Integration

- **Canonical sign:** `Σ`
- **Canonical name:** `Integration`
- **German working rendering:** Integration
- **Repository order:** 10 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Synthesis of disparate or conflicting elements into a more coherent whole.
- **Canonical dependencies:** `Χ` Distance, `Φ` Recontextualization
- **Structures provided in `PMS.yaml`:** `coherence`, `resolution_of_contradiction`, `multi_level_coordination`, `maturity_of_praxis`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Σ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.10 and §10.3

### Formal role

Coordinates disparate, layered, or conflicting structures into a more coherent functional configuration.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- coordination of conflicting roles or commitments
- integration of multiple local structures into a coherent configuration
- a repair or synthesis that preserves difference without treating it as resolved by label alone

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Integration may coordinate contradictory path loads, but it does not erase prior sequence, loss, asymmetry, or non-event structure.
- **SUB:** An integration-typed occurrence may be opened into the structures and relations it coordinates; Σ itself is not decomposed.
- **RETYPE:** Multiple local integrations may support a bounded higher-level integration-function without implying total coherence.
- **LIMITS:** Integration is not completeness, harmony, truth, lossless synthesis, PMS operator composition, or STRATA COMPOSE.

### Central non-equivalences

- Integration operator type ≠ STRATA COMPOSE
- PMS operator composition ≠ Σ
- integration ≠ aggregation
- integration ≠ conflict elimination
- integration ≠ completeness or truth

### Must not be claimed

- that integrated structure is complete, contradiction-free, empirically true, or normatively valid
- that Σ authorizes silent creation of a higher-level STRATA composite

### Paper-to-STRATA calibration note

The paper uses operator composition and Σ integration in different senses. STRATA adds a third distinct sense, COMPOSE, which creates declared analytical composites.

---

### 5.11 `Ψ` — Self-Binding

- **Canonical sign:** `Ψ`
- **Canonical name:** `Self-Binding`
- **German working rendering:** Selbstbindung
- **Repository order:** 11 of 11
- **Status:** PMS Base operator type; treated as structurally basic and non-derivative at its own level of description within the current PMS repository reference
- **Canonical definition:** Formation of self-relation through commitment to integrated structures across time.
- **Canonical dependencies:** `Σ` Integration, `Θ` Temporality, `Χ` Distance
- **Structures provided in `PMS.yaml`:** `self_model`, `responsibility_as_self_commitment`, `stable_normativity`, `autobiographical_coherence`
- **Primary control sources:**
  - `PMS.yaml` → `pms_model_reference.meta_axioms[id="Ψ"]`
  - supporting PMS explanatory source: `Praxeological Meta-Structure Theory.md` → §4.11 and §10.3

### Formal role

Binds integrated structures into a temporally extended self-relation, commitment structure, or stable policy relation.

### Typical operator occurrences

The following are occurrence families, not additional operator types:

- a commitment maintained across changing situations
- a role relation taken up as an enduring obligation
- a stable policy or constraint relation in an artificial system without implying consciousness

A valid occurrence remains reference-bound, context- or frame-bound, source-bound, and claim-bound. Competing typings must remain possible where the source structure supports them; they need not exist in every case. Reusing the operator name does not establish the occurrence.

### STRATA relation

- **PATH:** Self-binding occurrences may extend commitments and role continuity through time, but they do not make a trajectory an identity essence.
- **SUB:** A self-binding-typed occurrence may be opened into commitments, records, expectations, rules, and break costs that sustain it.
- **RETYPE:** Repeated commitments may support a bounded binding-function without converting the source object into Ψ.
- **LIMITS:** Self-binding is not consciousness, psychological identity, personality type, moral worth, diagnosis, or irreversible person attribution.

### Central non-equivalences

- Self-Binding operator type ≠ conscious selfhood
- self-binding occurrence ≠ permanent identity
- self-binding ≠ moral worth
- binding-function ≠ Ψ operator identity
- machine policy stability ≠ subjective experience

### Must not be claimed

- that a self-binding occurrence establishes consciousness, personality, essence, or permanent identity
- that an artificial system encoded with Ψ possesses subjective selfhood

### Paper-to-STRATA calibration note

The paper describes Ψ as fixpoint-like and connects it to self-modeling. This is explanatory language, not proof of mathematical closure, consciousness, or ontological selfhood.

---

## 6. Operator Occurrence Control

### 6.1 Minimal occurrence declaration

The Operator Index does not create a second Transformation Record. Before a concrete PMS operator occurrence is used as a STRATA source, the later chapter or record must provide an inspectable occurrence declaration. This index does not redefine machine fields. The current `07_model/Transformation_Record.schema.json` owns formal field names, requiredness, and encoding; the declaration must identify:

- the reference object or source structure;
- the proposed operator typing;
- the relevant context or frame;
- the temporal scope where applicable;
- the source basis;
- the typing justification;
- relevant competing typings, where supported;
- and the current confidence or record status.

This declaration does not prove empirical truth. It makes the typing inspectable. Formal field names, requiredness, and encoding remain the responsibility of the current Transformation Record Schema, while operator meaning and typing constraints remain controlled by PMS Base, governing prose, and their designated Reference owners.

### 6.2 Multiple typings

One reference object or source structure may support competing typings or participate in several distinct operator occurrences. A repeated non-decision may, for example, be reconstructed as a `Λ` occurrence, participate in an `Α` occurrence, and contribute to an `Ω` structure. These are distinct analytical relations, not automatically one occurrence with three interchangeable types. Such plurality does not merge the operator types and does not license silent level mixing.

### 6.3 Occurrence identity

An occurrence remains distinct from:

- the operator sign;
- the operator name;
- the abstract operator type;
- another occurrence with the same typing;
- a composite containing the occurrence;
- a derived function that the occurrence or composite later performs.

Nominal sameness is insufficient for reference continuity.

---

## 7. Operator Types across STRATA Operations

| STRATA operation | Operator types | Operator occurrences | Function/result relation |
|---|---|---|---|
| `COMPOSE` | may be referenced as typings; are not composed into a revised Base grammar | may be selected and related as source structures | no target function is assigned automatically |
| `DECOMPOSE` | never decomposed | may be opened into finer source-bound structures | no target function is assigned; the existing source or coarser function may be confirmed, refined, revised, partially preserved, or rejected |
| `PROJECT_AS` | remain unchanged | may serve as source objects where appropriate | a bounded contextual target function may be assigned only with source reference and origin type preserved |

Canonical rules:

```text
COMPOSE(operator occurrences)
≠
compose new PMS operator types
```

```text
DECOMPOSE(operator occurrence)
≠
decompose operator type
```

```text
PROJECT_AS(source object, operator-like function)
≠
replace origin type with PMS operator type
```

---

## 8. Derived Structures That Are Not Operators

The PMS paper and `PMS.yaml` contain systematically derived structures. STRATA additionally forms source-dependent derived analytical objects and bounded functions under the canonical Chapter 1 minimum burden. None extends Δ–Ψ.

| Family | Examples | Controlled status |
|---|---|---|
| PMS derived axes | Awareness, Coherence, Responsibility, Action/Enactment, Dignity-in-Practice | derived constructs, not independent primitives |
| PMS derived patterns | maturity constructions, asymmetry drift patterns, self-model chains | derived or interpretive formations, not operator types |
| Temporal ordering and sequence | temporal order, sequence | derived temporal relation or object claim requiring ordered source elements, a declared frame, and bounded reconstruction; not established by Θ alone |
| PATH objects | path, trajectory, branch structure, phase | derived analytical objects requiring additional COMPOSE structure, justification, and admissibility testing |
| SUB outputs | component map, internal temporal structure, revised source-function status | reconstructions of occurrences or composites, not primitives |
| RETYPE functions | frame-function, macro-event function, attractor-function, asymmetry-function, binding-function, integration-function, modulating function | bounded contextual functions, not operator identities |
| Profiles and weights | operator weighting, modulating profile, emergent functional profile | descriptions of relative load or configuration, not new operators |
| Remainders | non-operator remainder, semantic remainder, material remainder | disclosed non-capture or residual categories; they do not become operator candidates through STRATA |



Chapter 1 now requires every STRATA-derived object or function to retain identifiable source objects, a declared operation or chain, a bounded formation rule, constitutive source trace, analytical coordinates, loss, non-primitive status, and a stop condition. Analytical shorthand and repeated naming do not meet that burden.

```text
formation through declared source-dependent relation
≠
primitive creation

same label
≠
same reference object
```

The mere recurrence, usefulness, formalization, or machine-readability of a derived construct does not promote it into PMS Base. Any proposed operator addition or dependency revision requires an explicit, versioned PMS revision or a rival formalization.

---

## 9. Cross-Operator Controls

### 9.1 Weighting does not change dependency

An occurrence or composite may be described as `Ω`-dominant, `Λ/Θ`-heavy, `Χ/Σ`-stabilized, or otherwise weighted. Such descriptions concern local analytical load.

```text
operator weighting
≠
operator reordering
≠
dependency revision
```

### 9.2 Enabling is not establishing

A PMS operator may enable a later structure without being sufficient to establish it. Examples:

- `Θ` enables temporal ordering but does not establish a path or trajectory;
- `Α` enables recurrent stabilization but does not establish path dependence;
- `Ω` makes responsibility gradients structurally legible but does not establish a moral or legal responsibility verdict;
- `Χ` enables reflective distance but does not establish maturity;
- `Σ` enables integration but does not establish truth, completeness, or harmony;
- `Ψ` enables stable self-relation but does not establish consciousness or permanent identity.

### 9.3 Generativity is not exhaustive capture

Successful derivation or translation into Δ–Ψ may demonstrate formal tractability, explanatory usefulness, or internal coherence. It does not by itself demonstrate completeness, unique adequacy, empirical truth, semantic exhaustion, or superiority to rival grammars.

### 9.4 Structural analysis is not person analysis

Operator descriptions apply to scenes, enactments, roles, relations, configurations, paths, occurrences, and composites. They must not be converted into global person types, diagnoses, moral ranks, permanent identities, or irreversible labels.

### 9.5 Computational representation is bounded

Encoding operators, dependencies, or occurrences in YAML can validate declared structure and internal consistency. It does not implement praxis, prove subjective experience, establish causality, or validate a real-world application without calibration and external evaluation.

---

## 10. Historical Pre-Block Operator Gate

Before Foundations production begins, the Operator Index passes its local gate only if all of the following remain true:

1. Exactly eleven PMS Base operator types are listed: Δ, ∇, □, Λ, Α, Ω, Θ, Φ, Χ, Σ, Ψ.
2. Names, order, and direct dependencies match `PMS.yaml` exactly.
3. The direct dependency graph, linear reading path, and organizational operator layers are kept distinct and described as repository-reference structures, not uniquely final or ontological.
4. Operator sign, name, type, occurrence, composite, and target function remain distinct.
5. `COMPOSE`, `DECOMPOSE`, and `PROJECT_AS` are not presented as PMS Base operators.
6. No operator type is presented as decomposable by STRATA.
7. PMS operator composition, `Σ` Integration, and STRATA `COMPOSE` remain distinct.
8. `Θ` is recognized as supplying temporal ordering and sequence-capacity, but not as sufficient for a concrete STRATA sequence, path, trajectory, or path-dependence claim.
9. `Φ` is not treated as identical with `PROJECT_AS`.
10. Operator-like contextual functions preserve source reference and origin type.
11. Derived axes, profiles, temporal objects, paths, trajectories, functions, and remainder categories are not promoted to primitives.
12. No operator description authorizes person typing, diagnosis, moral rank, sanctions, or authority inheritance.
13. Formal representation remains distinct from empirical, causal, semantic, and normative validation.
14. Rival openness, calibration limits, failure, Stop, and Non-Capture remain possible.

---

## 11. Revision and Freeze Policy

This index originated as a pre-Block control artifact and remains a historical baseline. During chapter production it may gain:

- final chapter and section anchors;
- validated occurrence examples and counterexamples;
- links to operator-weighting and trajectory stress tests;
- links to the Formal Model and appendices;
- explicit case identifiers;
- refined usage notes where Blocks expose ambiguity.

It may not silently change:

- the eleven-operator inventory;
- canonical signs or names;
- order or direct dependencies;
- the non-decomposability of operator types;
- the distinction between origin type and target function;
- the prohibition on new primitives and authority inheritance.

Any proposed Base revision must be handled as an explicit, versioned PMS revision or rival formalization. It cannot be smuggled through a STRATA chapter, case, glossary entry, model field, or target function.

---

## Chapter 2 Frame-Coordinate Boundary

Chapter 2 WP1 defines the analytical frame coordinate. The following distinction is canonical for current Foundations production:

```text
`□`
→ operator sign

`Frame`
→ operator name / operator type reference

frame occurrence
→ concrete Chapter 1 operator-occurrence claim

analytical frame coordinate
→ bounded declaration of reconstruction relevance, inside/outside, object, and claim
```

An analytical frame may be supported by Frame occurrences, but it is not the abstract operator type and need not be reducible to one isolated occurrence. Naming `□` does not complete the coordinate declaration. This boundary is defined in [`Chapter 2 WP1`](../01_blocks/01_foundations.md#chapter-2-frame-granularity-and-relative-level), does not modify the PMS Base meaning of Frame, and does not create a new operator.

---

## Chapter 2 Provisional-Lock Frame Boundary

Chapter 2 is now provisionally locked and preserves the PMS Base Frame operator unchanged.

```text
`□`
→ PMS operator sign

`Frame`
→ PMS operator name and type

Frame-typed occurrence
→ concrete occurrence claim under Chapter 1 burdens

analytical frame
→ Chapter 2 coordinate declaring reconstruction relevance
```

An analytical frame may be supported by one or more Frame-typed occurrences, but no such relation converts the coordinate into a new PMS operator or alters the Δ–Ψ dependency order. A changed analytical frame also does not automatically establish a `Φ` occurrence or `PROJECT_AS` operation.

---

## Chapter 3 WP1 — Temporal Operator Constraints

Canonical Chapter 3 Sections 3.1–3.5 fix the following temporal-object operator boundaries without changing PMS Base:

```text
Θ supports temporal ordering
but Θ alone does not establish:
trajectory
sedimentation
path dependence
teleology
irreversibility
```

Stronger historical-load claims may draw on source-supported relations such as:

| Relation | Possible temporal load | Not an automatic result |
| --- | --- | --- |
| `Α + Θ` | recurrent stabilization over time | sedimentation or trajectory by formula |
| `Ω + Θ` | accumulated asymmetric cost/exposure | causal proof or historical necessity |
| `Ψ + Θ` | persistent or increasingly costly binding | path dependence automatically |
| `Λ + Θ` | accumulated non-realization, delay, residue, or closure pressure | non-event without expectation support |

`Φ` may change later legibility but does not erase prior sequence or load. `Σ` may integrate a reconstruction but does not prove that the selected history is complete. `Χ` may preserve distance and stop capability but does not itself establish reversibility.

These are reconstruction constraints, not new dependencies or operator definitions.

## Chapter 3 WP2 — Ordered Historical Operator Constraints

Canonical source: [`Chapter 3 Sections 3.6–3.8`](../01_blocks/01_foundations.md#chapter-3-configuration-event-non-event-transition-path-and-trajectory).

- `Θ` supports temporal ordering but does not by itself establish sequence selection, actual path traversal, trajectory, sedimentation, teleology, or path dependence.
- `Λ`-typed non-event occurrences may become path- or trajectory-relevant only where their expectation, bounded non-realization, and later load are source-supported.
- `Α + Θ`, `Ω + Θ`, `Ψ + Θ`, and `Λ + Θ` are candidate reconstruction prompts for historical carry-over, not sufficient formulas.
- Operator co-presence does not identify a path, trajectory, or `COMPOSE` occurrence.
- Directional stabilization does not convert `Α` or `Θ` into teleological authority.

```text
operator occurrence pattern
≠ temporal object proof
≠ operation identity
≠ historical necessity
```

## Chapter 3 WP3 — Historical Property Operator Constraints

Canonical source: [`Chapter 3 Sections 3.9–3.13`](../01_blocks/01_foundations.md#chapter-3-configuration-event-non-event-transition-path-and-trajectory).

```text
Θ supports temporal order
but Θ alone does not establish:
path dependence
sedimentation
irreversibility
alternative availability
```

- `Α + Θ`, `Ω + Θ`, `Ψ + Θ`, and `Λ + Θ` remain candidate carrier prompts, not formulas or sufficient conditions.
- A path-dependence claim additionally requires current-conditions insufficiency, prior-order or branch relevance, supported counterfactual sensitivity, and traceable carry-over.
- Sedimentation requires an occurrence-level carrier and a declared later praxis effect.
- `Φ` may recontextualize later meaning but does not erase prior load or establish restoration.
- `Χ` may preserve distance and Stop capability but does not prove reversibility.
- `Σ` may integrate a reconstruction but does not establish historical completeness or one master trajectory.
- Unrealized alternatives remain source-supported historical relations, not new operator types or free counterfactuals.

```text
operator relation
≠ historical-property proof
≠ causal proof
≠ operation identity
```

---

## Chapter 3 Temporal Operator Boundary

The provisionally locked [`Chapter 3`](../01_blocks/01_foundations.md#chapter-3-configuration-event-non-event-transition-path-and-trajectory) preserves the operator boundary: `Θ` supports temporal ordering but does not alone establish event, transition, path, trajectory, sedimentation, irreversibility, or path dependence. `Λ` requires a supported expectation frame and is not missing information. Pairings such as `Α + Θ`, `Ω + Θ`, `Ψ + Θ`, or `Λ + Θ` may support stronger claims only when their historical load is source-traceable.

---

## Chapter 4 Preparation — PMS Operator / STRATA Operation Boundary

The Chapter 4 preparation architecture preserves two closed but different inventories:

```text
PMS Base operator types
Δ through Ψ
= eleven ordered operator types
```

```text
STRATA core operations
COMPOSE / DECOMPOSE / PROJECT_AS
= three transformation-operation types
```

The counts answer different questions and may not be collapsed.

- `DECOMPOSE` applies to concrete operator-typed occurrences or composites, never to the abstract Δ–Ψ types.
- `COMPOSE` may form composites containing operator-typed occurrences without creating a new operator type.
- `PROJECT_AS` may assign a bounded contextual function to an origin-typed object without retyping that object or adding a PMS primitive.
- `Φ` Recontextualization remains a PMS operator type; it is not identical to `PROJECT_AS`.
- `Σ` Closure may participate in source structures; it is not a STRATA operation or proof of transformation completion.

```text
operator inventory
≠ operation inventory

operator pairing
≠ operation identity

operation application
≠ Base revision
```

A genuine irreducible pressure outside the three STRATA signatures must remain explicit as non-capture or PMS Base / STRATA architecture revision pressure; it may not be hidden as an undeclared fourth operation.

---

## Chapter 4 WP1 Canonical Operator / Operation Return

Chapter 4 WP1 now canonically fixes that abstract Δ–Ψ operator types are not operation sources for `DECOMPOSE`. Concrete operator-typed occurrences and composites may be transformed where their operation signature is otherwise satisfied.

```text
operator type
≠ operation type

operator-typed occurrence
≠ operator type

PROJECT_AS
≠ Φ Recontextualization

COMPOSE
≠ Σ Integration
```

Canonical return: [`Chapter 4 WP1`](../01_blocks/01_foundations.md#chapter-4-the-three-strata-operations-compose-decompose-and-project_as).

---

## Chapter 4 Operator / Operation Lock Boundary

The provisionally locked [`Chapter 4`](../01_blocks/01_foundations.md#chapter-4-the-three-strata-operations-compose-decompose-and-project_as) preserves the closed distinction between the eleven Δ–Ψ PMS operator types and the three STRATA operation types. Operator types are never decomposed; only concrete occurrences and composites may become operation sources. `PROJECT_AS` never converts a source object into a new operator type or primitive.

---

## Chapter 5 Preparation — Origin Type / PMS Operator Boundary

Chapter 5 must prevent the term **origin type** from being read as a synonym for **PMS operator type**.

```text
PMS operator type
→ one abstract Δ–Ψ type
```

```text
origin type
→ the declared analytical typing of the source object
  in its source reconstruction
```

An origin type may be:

- a concrete operator-typed occurrence;
- a composite structure;
- a configuration, event, non-event, or transition;
- a sequence, path, or trajectory;
- another warranted derived analytical object.

`PROJECT_AS` preserves that origin type while assigning a contextual target function. A trajectory performing a frame-function does not become the abstract `Frame` operator type. A path performing a memory-function does not create a new PMS operator or primitive.

```text
functional similarity
≠ operator identity

projected function
≠ Base revision
```

A genuine operator-identity claim remains governed by `PMS.yaml` and the operator-occurrence burden. Chapter 5 may not infer or revise the Δ–Ψ inventory through projection language.

---

## Chapter 5 WP1 Operator-Language Boundary

Chapter 5 WP1 prevents target-function labels from rewriting source typing as an abstract Δ–Ψ operator type.

```text
path performing a bounded frame-function
≠ path becoming the Frame operator type

path performing a bounded memory-function
≠ path becoming the Memory operator type
```

Operator language may name a bounded contextual function only when origin type, target context, source trace, and validity scope remain explicit. It creates no new operator occurrence automatically and no authority inheritance.

Canonical return: [`Chapter 5 WP1`](../01_blocks/01_foundations.md#chapter-5-origin-type-target-function-and-transformation-context).

## Chapter 5 WP2 Operator-Type Integrity Handoff

Chapter 5 WP2 uses PMS operator names only as controlled comparison points. A source object that performs a frame-, memory-, attractor-, or other operator-related contextual function does not thereby become the corresponding abstract operator type.

```text
same referent
+ operator-like function
≠ operator-type identity
```

A type revision involving an operator-typed occurrence requires new source support and an explicit revised typing claim. It may not be inferred from resemblance, utility, or a later target function.

Canonical return: [`Chapter 5 §5.5`](../01_blocks/01_foundations.md#chapter-5-origin-type-target-function-and-transformation-context).

## Chapter 5 WP3 Operator and Temporal-Function Boundary

Historical carry-over, contextual persistence, or a later target function does not convert a path or trajectory into an abstract operator type. A source object may perform a bounded Frame-, Memory-, Attractor-, or other operator-related function only while origin type, target context, temporal trace, and validity scope remain explicit.

```text
historically formed function
≠ operator-type identity
≠ timeless property
```

Canonical return: [`Chapter 5 §§5.7–5.9`](../01_blocks/01_foundations.md#chapter-5-origin-type-target-function-and-transformation-context).

---

## Chapter 5 Operator-Type and Function Boundary

Chapter 5 confirms that an origin type need not be an abstract Δ–Ψ operator type and that a contextually named function does not create operator identity.

```text
path performing a frame-function
≠ Frame operator type

trajectory performing a memory-function
≠ Memory primitive
```

Δ–Ψ remain unchanged. `PROJECT_AS` preserves origin type, creates no new PMS primitive, and grants no authority inheritance. Canonical return: [`Chapter 5`](../01_blocks/01_foundations.md#chapter-5-origin-type-target-function-and-transformation-context).

---

## Chapter 6 Admissibility / PMS-Operator Boundary

Chapter 6 does not rank, score, decompose, extend, or retype the eleven Δ–Ψ PMS operator types. The Admissibility Band tests delimited STRATA transformation claims and their source load; it does not grade operator types or convert analytical usefulness into PMS Base authority.

```text
operator type
≠ admissibility score
≠ authority rank
```

Canonical return: [`Chapter 6`](../01_blocks/01_foundations.md#chapter-6-the-strata-admissibility-band).

---

## Chapter 7 WP4 Provisional-Lock Synchronization

Chapter 7 is provisionally locked after integrated ownership, redundancy, status, case-duty, prose-to-schema, link, schema, fingerprint, package, and roundtrip audit. The Shared Transformation Record records transformation claims without becoming the transformation, a truth proof, or an authority source. The existing record schema supplies lower-authority structural carriers; its Chapter-7 handoff annotation does not redefine canonical prose. The sixteen Chapter-7 case identifiers remain later production duties rather than completed evidence. Canonical return: [Chapter 7](../01_blocks/01_foundations.md#chapter-7-shared-transformation-record).

---

## Chapter 8 Preparation PMS-Operator Non-Equivalence Handoff

Chapter 8 preparation preserves these operator boundaries:

```text
projection
≠ operator identity

operator weighting
≠ operator replacement

contextual operator-like function
≠ new PMS primitive
```

A configuration may weight an operator occurrence strongly, and an origin-typed object may perform a bounded operator-related function, without renaming, reordering, bypassing, decomposing, extending, or replacing any Δ–Ψ operator type. The Chapter 8 catalogue audits these collapses but does not redefine PMS Base. Production control: [`Chapter 8 Preparation Record`](Chapter_8_Preparation_Record.md).

---

## Chapter 8 WP1 Operation-Boundary Synchronization

Canonical Sections 8.3–8.4 now audit two operation confusions:

```text
COMPOSE
≠ lossless addition

DECOMPOSE
≠ discovery of final constituents
```

`COMPOSE` may create a real composite while preserving selection and loss. `DECOMPOSE` may reconstruct a concrete occurrence or composite more finely while remaining frame-, source-, reference-, and claim-bound. Neither operation changes the Δ–Ψ inventory, and abstract operator types remain non-decomposable.

---

## Chapter 8 WP2 Operator and Grammar Synchronization

Canonical Sections 8.8–8.10 protect operator identity across projection and weighting:

```text
origin type
≠ target function

PROJECT_AS result
≠ operator identity

operator weighting
≠ operator replacement
```

No contextual function, repeated projection, weighting profile, dominant occurrence, or named derived object may rename, reorder, bypass, decompose, extend, or replace Δ–Ψ. PMS Base remains the sole owner of operator identity and dependencies.

---

## Chapter 8 WP3 Analogy, Recursion, and Authority Synchronization

Canonical Sections 8.11–8.13 preserve operator and authority boundaries across analogy and repeated transformations:

```text
operator-like analogy
≠ operator identity

recursive operation chain
≠ new operation kind

machine-valid operator declaration
≠ authority to revise PMS Base
```

Analogy may remain `analogy_only`; every recursive occurrence retains exactly one of the three STRATA operation kinds; Δ–Ψ revision and authority inheritance remain prohibited.

---

## Chapter 8 WP4 Operator and Grammar Lock

Chapter 8 is provisionally locked without changing PMS Base. The catalogue protects operator boundaries but does not own operator definitions.

- projection remains distinct from operator identity;
- operator weighting remains distinct from operator replacement;
- recursion remains distinct from completeness;
- legibility remains distinct from authority.

No Chapter-8 finding renames, reorders, decomposes, adds, deletes, or replaces Δ–Ψ. PATH inherits this unchanged grammar.

---

## Chapter 9 Preparation — Θ, Λ, and Φ PATH-Entry Boundary

| PMS operator | Chapter 9 permitted use | Prohibited inference |
| --- | --- | --- |
| `Θ — Temporal Structuring` | order, duration, delay, repetition, persistence, accumulation, bounded irreversibility, historical position | `Θ` alone establishes transition, path, trajectory, sedimentation, path dependence, causality, or authority |
| `Λ — Non-Event` | separately tested expectation-bound non-realization where delay or absence is structurally relevant | missing information, elapsed time, silence, or every delay is `Λ` |
| `Φ — Recontextualization` | separate operator-occurrence claim where a structure is placed in a new frame | every later change in legibility is identical to abstract `Φ`; prior sequence is erased |

```text
operator type
≠ operator occurrence
≠ temporal coordinate
≠ transition object
≠ STRATA operation
```

The Chapter 9 Preparation Gate does not revise the Δ–Ψ inventory, order, dependencies, names, or type identities. Production control: [`Chapter 9 Preparation Record`](Chapter_9_Preparation_Record.md).

---

## Chapter 9 WP1 — Canonical Θ Boundary

Chapter 9 §9.2 uses `Θ` according to PMS Base while preserving:

```text
Θ operator type
≠ Θ occurrence
≠ temporal coordinate
≠ temporal object
≠ STRATA operation
```

Permitted WP1 use includes temporal position, relative or partial order, interval declaration, and identification of later duration/repetition/persistence candidates. Prohibited inference includes automatic event/non-event identity, transition, path, trajectory, sedimentation, path dependence, causality, target function, or authority.

Canonical route: [`Chapter 9 §9.2`](../01_blocks/02_part_i_path.md#9-2-theta-as-temporal-structuring). No Δ–Ψ identity, order, dependency, or inventory entry was changed.

---

## Chapter 9 WP2 — Θ, Λ, and Φ Boundaries

- `Θ` supports interval, duration, delay, persistence, bounded irreversibility, and temporal position declarations but does not establish stronger historical objects by itself.
- `Λ` may type a concrete structured non-realization only where expectation, bounded condition, non-realization, and praxis load are supported.
- `Φ` remains the PMS Recontextualization operator type; a temporal-recontextualization relation is not automatically a `Φ` occurrence.
- None of these operator uses selects `COMPOSE`, `DECOMPOSE`, or `PROJECT_AS` automatically.

Canonical route: [`Chapter 9 §§9.5–9.9`](../01_blocks/02_part_i_path.md#9-5-duration). No Δ–Ψ identity, order, dependency, or inventory entry changed.

---

## Chapter 9 WP3 — Operator and Transition Boundary

A transition candidate or warranted transition is a temporal object/relation, not a PMS operator type, operator occurrence, or STRATA operation.

- `Θ` may support temporal-order and interval declarations but does not establish the transition relation alone.
- `Λ` may contribute a separately warranted non-event occurrence but does not convert the whole transition into absence.
- `Φ` may be separately typed where a concrete recontextualization occurrence is claimed; frame handoff is not automatically `Φ`.
- No transition field changes Δ–Ψ, admits a new primitive, or selects `COMPOSE`, `DECOMPOSE`, or `PROJECT_AS` automatically.

Canonical route: [`Chapter 9 §§9.10–9.12`](../01_blocks/02_part_i_path.md#9-10-transition-preconditions).


## Chapter 9 WP4 — Operator and Transition Lock Boundary

Chapter 9 is provisionally locked without changing any Δ–Ψ operator identity, order, or dependency. `Θ` supports temporal structuring but does not create transition, path, trajectory, path dependence, or authority by itself. Transition remains an analytical temporal relation, not an operator type or STRATA operation.

```text
Θ support
≠ transition sufficiency
≠ operation selection
```

Canonical return: [`Chapter 9`](../01_blocks/02_part_i_path.md#chapter-9-temporal-order-and-transition).

---

## Chapter 10 Preparation — PMS Operator Material and Path Boundary

A path may contain configurations in which multiple Δ–Ψ operator occurrences are reconstructed over time. This does not change operator type, order, dependency, or inventory.

| PMS-related material | Permitted path use | Prohibited inference |
| --- | --- | --- |
| operator occurrence in configuration | component of a source or intermediate configuration | occurrence becomes path, operation, or new primitive |
| `Θ`-supported temporal relation | supports order, duration, delay, persistence, and position fields | `Θ` alone establishes path, trajectory, or dependence |
| `Λ`-supported non-event occurrence | may contribute to a transition or block/defer continuation | missing record or every absence is `Λ` |
| `Φ`-typed occurrence where separately warranted | may recontextualize earlier structure | frame change automatically equals abstract `Φ` or `PROJECT_AS` |
| operator weighting profile | may vary across configurations and contribute to path comparison | weighting becomes operator replacement or person type |

```text
operator-occurrence chain
≠ path automatically

path object
≠ PMS operator
≠ STRATA operation
```

The Preparation Gate does not revise Δ–Ψ. Production control: [`Chapter 10 Preparation Record`](Chapter_10_Preparation_Record.md).

---

## Chapter 10 WP1 — PMS Occurrence Material and Path Object Status

Chapter 10 may use source-supported PMS operator occurrences as component material in configurations, transitions, event clusters, non-event structures, bindings, asymmetries, temporal relations, and recontextualizations. The path remains a derived temporal object rather than a PMS operator type.

```text
operator type
≠ operator occurrence
≠ path component role
≠ path object
```

An operator occurrence may persist, change weight, interact with other occurrences, or help constitute a transition. Path membership does not rename, replace, decompose, or extend Δ–Ψ. The path does not become an operator merely because several occurrences are temporally connected.

Canonical route: [`Chapter 10 §§10.1–10.6`](../01_blocks/02_part_i_path.md#chapter-10-path). No PMS operator identity, dependency, order, or inventory entry was changed.

---

## Chapter 10 WP2 — Operator Occurrences and Qualified Path Status

PMS operator occurrences may help reconstruct availability, obstruction, non-realization, temporal load, asymmetry, commitment, or recontextualization within a path-status claim. They do not mechanically determine realized, blocked, aborted, or deferred status.

```text
Λ occurrence
≠ blocked continuation automatically

Θ-supported delay
≠ deferred continuation automatically

Ω occurrence
≠ proof of blocking agency

Φ occurrence
≠ erased prior path status
```

A status claim remains a derived, frame- and source-bound historical relation. No PMS operator type, ordering, dependency, or identity is changed. Canonical route: [`Chapter 10 §§10.7–10.10`](../01_blocks/02_part_i_path.md#10-7-realized-path).

